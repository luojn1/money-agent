# 前端通过 Vite 代理访问后端时可留空
VITE_API_BASE_URL=
VITE_USE_MOCK_PIPELINE=false

# 后端监听端口
PORT=8080

# 单容器部署时由后端托管前端静态资源
SERVE_FRONTEND=true
RUNTIME_ROOT=/tmp/money-agent-runtime
PYTHON_BIN=/opt/venv/bin/python

# 前后端分域部署时配置，逗号分隔；同源部署可留空
CORS_ORIGIN=
