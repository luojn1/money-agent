# D 第二阶段改动清单

> `交付代码/` 里是**新增文件**（路径即目标路径，可直接放进仓库）。
> 下面 4 个是**在现有文件上的最小改动**，已合并进 money-agent-main，此清单供 A review。

## 新增文件（交付代码/）

| 文件 | 作用 |
| --- | --- |
| `website/backend/src/services/reportContextRetriever.ts` | 报告上下文检索（纯规则，citation 来源） |
| `website/backend/src/services/chatOrchestrator.ts` | 对话编排：脱敏 + 模板/LLM + 历史 + 降级 |
| `website/frontend/src/components/ChatPanel.tsx` | 报告页聊天入口组件 |
| `website/frontend/src/components/ChatPanel.css` | 面板样式（`mchat-` 前缀独立文件，不碰 styles.css） |
| `scripts/verify-chat.ts` | T3/T6 验证脚本（31 项断言） |
| `start-demo.sh` | 一键本地演示脚本（自动加载 .env.local 里的 LLM 密钥，未配置则用模板回答） |

## 改动文件（最小新增，未改现有逻辑）

1. **`website/backend/src/routes/pipeline.ts`**
   - import 增加 `ChatError, getChatHistory, handleChat`
   - 文件末尾新增 `POST /:taskId/chat`（错误码 400/404/409）和 `GET /:taskId/chat/history`
   - 未动 `/analyze`、`/status`、`/result`
2. **`website/frontend/src/services/pipelineApi.ts`**
   - 新增类型 `ChatCitation / ChatAnswer / ChatMessage` 和方法 `sendChat`、`getChatHistory`
3. **`website/frontend/src/pages/ReportPage.tsx`**
   - import `ChatPanel`；`</main>` 后新增一行 `<ChatPanel taskId={taskId} />`
4. **`package.json`（根）**
   - scripts 新增 `"verify:chat"`
5. **`website/backend/src/services/pipelineOrchestrator.ts`**（2026-07-10 补充）
   - D Agent 的 spawn 参数末尾新增一项：后端配置了 `LLM_API_KEY` 时追加 `--llm`，
     让报告"建议落地"文案走 D 已有的 `engine/llm_polish.py` 润色（失败静默回退模板）。
     仅 1 行条件展开，未动 B/C 调用与报告组装逻辑。
6. **`agents/recommendation_action/engine/recommender.py`**（2026-07-10 补充，D 自己的模块）
   - 建议动作化：不再复读 questionToAsk（问题清单已有原句），改为按风险类别写"具体怎么做"
     （核对到账、留存证据、演示截图等），建议与问题清单重复从 8 条降到 0 条
   - 理由差异化：C 的通用兜底句"该条款可能增加…成本"按类别替换为具体后果；
     "命中规则X"机器腔改写为"合同里存在X的情形"
   - 同类合并：相同 action 的建议合并为一条（风险关联取并集、优先级取高档），10 条 → 8 条
   - 协议字段、ID 规则、relatedRiskIds 关联全部不变；pytest 16 项、verify:bcd-pipeline 均通过

## 接口冻结遵守

- ✅ 未改 `/analyze /status /result`；未改 `shared/*`；未动 `types/pipeline.ts`；样式独立文件

## 验证（2026-07-10 实测）

```bash
pnpm --filter @money-agent/backend run typecheck   # ✓
pnpm --filter @money-agent/frontend run typecheck  # ✓
pnpm run verify:chat                                # ✓ 18/18
pnpm run verify:bcd-pipeline                        # ✓ 真实 B→C→D：8 风险 10 建议
# 浏览器实测：上传合同 → 报告页 → 问一问 → 回答带 risk/clause/case 引用 ✓
# LLM 模式实测（DeepSeek，已充值 10 元）：单轮 1~2 秒；多轮追问带上下文
#（如先问"还款压力大吗"再补"我工资15000"，能延续话题估算），跑题问题礼貌拉回 ✓
```

## 与 B/C 第二阶段更新的关系（给 A）

- **C 新包**（money-agent-feature-plain-language-scenario）：`agents/risk_case` 与整合仓库已一致；
  她的 `A_integration_package` 含 shared 协议补丁和对 `agents/recommendation_action`（D 模块）的
  场景化建议改动（`scenario_recommender.py` 等），按约定由 A 协调合并，D 未擅自合入。
- **B 新包**：改动集中在前端报告页（T1/T5：`ReportSummary`/`CaseReferenceCard`/`reportViewModel` 等）。
  与 D 的冲突点正是分工文档预告的三个文件：`pipeline.ts`（B 无 chat 路由）、`pipelineApi.ts`、
  `ReportPage.tsx`——D 的改动均为"末尾新增"，合并时保留双方新增即可。
