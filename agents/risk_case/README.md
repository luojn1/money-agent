# Risk Case Agent

该目录用于存放 C Agent：风险识别、规则匹配、案例检索与风险输出模块。

预期输入：
- B Agent 生成的 ContractCostOutput

预期输出：
- RiskCaseOutput

后续需要从 c-agents-github-package 分支迁入完整代码，包括 main.py、db、rag、rules、knowledge、tests 和 requirements.txt。
